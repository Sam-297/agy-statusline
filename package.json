{
  "name": "agy-statusline",
  "version": "1.0.0",
  "description": "A zero-dependency, instant status line for the Antigravity CLI",
  "bin": {
    "agy-statusline": "bin/agy-statusline"
  },
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "repository": "Sam-297/agy-statusline",
  "scripts": {
    "test": "node --test"
  },
  "author": "Sam",
  "license": "MIT",
  "lint-staged": {
    "*.js": [
      "eslint --fix",
      "prettier --write"
    ],
    "*.{json,md}": [
      "prettier --write"
    ]
  },
  "devDependencies": {
    "eslint-config-prettier": "^10.1.8"
  }
}
